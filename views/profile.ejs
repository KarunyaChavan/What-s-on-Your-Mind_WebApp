<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Profile</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body>
        <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
            <div class = "flex items-end w-full justify-end">
                <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout">Logout</a>
            </div>
            <h3 class = "text-3xl "><span class = "font-light">Hello,</span> <%= user.name %>👋🏼</h3>
            <h5 class="mb-5">You can create a new post</h5>
            <form action="/post" method="post">
                <textarea placeholder="What's on your mind ?" class="block rounded-md mb-2 p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
                <input class="text-sm px-3 w-40 block rounded-md mb-2 px-3 py-2 bg-blue-500" value="Create New Post" type="submit">
            </form>
            <div  class="posts">
                <h3 class="text-zinc-400">Your Posts.</h3>
                <div class="postcontainer mt-5">
                    <% user.posts.reverse().forEach(function(post){ %> 
                    <div class="post w-1/3 p-4  rounded-md mb-4 border-[1px] border-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%= user.username%></h4>
                        <p class="text-sm tracking-tight">
                            <%= post.content %>
                        </p>
                        <small class="mt-1 inline-block"><%= post.likes.length %> likes</small>
                        <div class="btns flex gap-4 mt-2">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-600 " href="/edit/<%= post._id %>">Edit</a>
                        </div>
                    </div>
                    <% }) %>
                </div>
            </div>
        </div>
    </body>
</html>